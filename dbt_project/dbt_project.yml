name: 'health_observability'
version: '1.0.0'
config-version: 2

profile: 'health_observability'

model-paths: ["models"]
test-paths: ["tests"]

clean-targets:
  - "target"
  - "dbt_packages"

models:
  health_observability:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
    observability:
      +materialized: view
      +schema: observability